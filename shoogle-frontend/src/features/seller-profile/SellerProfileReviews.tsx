import React from "react";

// This component intentionally renders nothing.
// Reviews are now shown only in the designated section below the listings page.

const SellerProfileReviews = ({ reviews }: { reviews: any[] }) => {
  return null;
};

export default SellerProfileReviews;
